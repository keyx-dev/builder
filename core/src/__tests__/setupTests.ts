import { expect } from "vitest";
import "@testing-library/jest-dom"; // Import the main library for side effects

expect.extend({}); // Extend with an empty object for now, matchers should be available globally
